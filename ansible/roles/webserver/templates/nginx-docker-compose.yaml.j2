version: '3.7'

services:

    nodejs:
        build: ./node
        volumes:
            - ./node:/usr/src/server
        ports:
            - "80:3000"
        restart: always
        container_name: nodejs



